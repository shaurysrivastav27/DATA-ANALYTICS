df<-read.csv("/home/shaury/Downloads/nptel/winequality-red.csv")
 #View(df)
 summary(df$quality)
 library(caTools)
 set.seed(123)
 sample<-sample.split(df,SplitRatio=0.8)
 train<-subset(df,sample==TRUE)
 test<-subset(df,sample==FALSE)
 library(class)
 train<-subset(df,sample==TRUE)
 log1<-glm(train$quality~.,family=gaussian,train)
 train=test
 pred<-predict(log1,train)
 summary(log1)
 train=subset(df,sample==TRUE)
 log1<-glm(train$quality~volatile.acidity+chlorides+total.sulfur.dioxide+sulphates+alcohol,family=gaussian,train)
 summary(log1)
 train=test
 pred=predict(log1,train)
 pred=data.frame(pred)
 pred=round(pred,0)
 table(pred$pred,test$quality)
 mean(test$quality==pred)

